# License TODO

本仓库**尚未选择许可证**，当前不授予任何再分发/修改的法律许可（除 GitHub 默认服务条款外）。

公开分发前请仓库所有者（@HuKernel）选定 LICENSE，例如 MIT / Apache-2.0 / CC-BY-4.0（内容型仓库常用），然后把本文件替换为正式的 `LICENSE` 文件，并同步：

1. `plugin.json` 与 `.claude-plugin/plugin.json` 的 `license` 字段
2. `README.md` 的 License 节

在此之前，安装器与 scripts 仅为本仓库克隆者的本地便利工具。
